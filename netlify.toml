# Netlify est en pause : la publication se fait sur GitHub Pages.
# Sécurité supplémentaire si les builds Netlify sont réactivés par erreur.
[build]
  ignore = "exit 0"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
